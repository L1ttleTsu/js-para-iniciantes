/*O operador ternário tem esse nome porque ele possui
3 operandos/coisas acontecendo dentro dele*/
idade = 15;
var podeBeber = (idade >= 18) ? "Pode beber" : "Não pode beber";
// condição ? true : false
/* você passa uma condição o pede o valor boleano dela, depois indica o que ele faz
se for verdadeiro e o que faz se for falso.*/
var scroll = 1000; 
scroll += 500;

var possuiCarro = true;
var possuiCasa = true;
var darCredito = (possuiCarro && possuiCasa) ? "Credito aprovado" : "Credito negado"